import { Button } from '@/components/ui/button';

const SaveAction = () => {
    return (
        <>
            <Button variant="ghost">Save</Button>
        </>
    );
};

export default SaveAction;
