import { Button } from '@/components/ui/button';

const SaveAction = () => {
    return (
        <>
            <Button variant="outline">Save</Button>
        </>
    );
};

export default SaveAction;
